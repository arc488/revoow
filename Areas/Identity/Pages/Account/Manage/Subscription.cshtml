@page
@model SubscriptionModel
@{

    ViewData["Title"] = "Subscription";
    ViewData["ActivePage"] = ManageNavPages.Subscription;
}

    <div class="container">
        <h4>@ViewData["Title"]</h4>
        <input id="currentPlanValue" value="@Model.CurrentType" />
        <input id="isCanceled" value="@Model.IsCanceled.ToString()" />

        <div class="information">
            <p class="currentplan">
                You are currently subscribed to the @Model.SubscriptionType.ToString() plan.
            </p>
            @if (Model.SubscriptionType != SubscriptionType.Starter)
            {
                <p class="expiration">
                    Your subscription will renew on @Model.Expiration.
                </p>
            }

            @if (Model.IsCanceled)
            {
                <p class="cancelInfo">
                    Your subscription has been cancelled or downgraded to Start plan and will expire on @Model.Expiration.
                </p>
            }

        </div>
        <hr>

        <div class="upgrade">
            <h4>Upgrade Plan</h4>
            <partial name="_ChoosePlanPartial" />

            <form method="post">
                <div class="form-group">
                    <select hidden asp-for="Input.SubscriptionType" asp-items="Html.GetEnumSelectList<SubscriptionType>()">
                        <option selected="selected" value="">Please select</option>
                    </select>
                </div>
                <button type="submit" class="btn btn-primary">Upgrade</button>
            </form>
        </div>

        <hr>
        <div class="cancel">
            <h4>Cancel Subscription</h4>
            <br>
            <form>
                <div class="form-group col-md-4">
                    <label asp-for="Input.Password">Password</label>
                    <input asp-for="Input.Password" class="form-control" />
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>
                <div class="form-group col-md-4">
                    <input type="submit" class="btn btn-danger">
                </div>
            </form>
        </div>
    </div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}